//import withAuth from '@/components/AuthRoute';
import React from 'react';

const products = () => {
  return <div className="text-3xl font-bold text-blue-600">products page</div>;
};

export default products;
//export default withAuth(products);
